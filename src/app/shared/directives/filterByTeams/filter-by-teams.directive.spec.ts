import { FilterByTeamsDirective } from './filter-by-teams.directive';

describe('FilterByTeamsDirective', () => {
  it('should create an instance', () => {
    const directive = new FilterByTeamsDirective();
    expect(directive).toBeTruthy();
  });
});
