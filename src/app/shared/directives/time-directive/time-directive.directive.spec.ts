import { TimeDirectiveDirective } from './time-directive.directive';

describe('TimeDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TimeDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
